[project]
name = "conditional-equivalence-reproduction"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
  "numpy==2.3.2",
  "z3-solver==4.15.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["reproduction"]
